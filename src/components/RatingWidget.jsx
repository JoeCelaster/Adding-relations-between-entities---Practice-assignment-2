import React from "react";

const RatingWidget = ({name, description, image, avgRating, totalRatings}) => {
  return (
    <div>
        <form action="">
            <input type="text" name="" id="" />
            <button>give ur rating</button>
        </form> 
    </div>
  );
};

export default RatingWidget;
